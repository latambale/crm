/* config.js : generated per environment */
//window.API_BASE = "http://127.0.0.1:8000";  // or actual backend IP:PORT if different

window.API_BASE = "/api";      // when backend is reverse-proxied under the same domain
 //window.API_BASE = "https://api.elated-forest-72977.pktriot.net";  // ← if you host API on a sub-domain
